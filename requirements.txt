# Django
Django>=5.2.16,<6.0
djangorestframework>=3.15,<4.0
djangorestframework-simplejwt>=5.3,<6.0
django-cors-headers>=4.4,<5.0
gunicorn>=22.0,<24.0
# ASGI. L'agent v2 tourne sur asyncio (PydanticAI) et asyncio ne cohabite pas
# avec gevent: tous les greenlets partagent un thread systeme, et asyncio suit
# sa boucle par thread. Mesure du 2026-08-27 sur 8 conversations concurrentes
# sous gevent: 1 passe, 7 echouent. Voir planner/asgi.py.
uvicorn[standard]>=0.30,<1.0
gevent>=24.0,<26.0
whitenoise>=6.6,<7.0

# Database
psycopg2-binary>=2.9,<3.0
psycogreen>=1.0,<2.0
dj-database-url>=2.1,<3.0

# AI & Document Processing
anthropic>=0.49,<1.0
google-genai>=1.75,<1.76  # pinned narrowly: tool-calling validated on this line (avoid schema-validation drift between 1.6x/1.7x)
openai>=1.51,<2.0  # DeepSeek provider (OpenAI-compatible API, base_url https://api.deepseek.com)
# Agent v2 (services/agent_v2). Les TROIS epinglages tiennent ensemble, mesure
# du 2026-08-24: sans opentelemetry-api<1.38, `import pydantic_ai` casse sur
# ModuleNotFoundError: opentelemetry._events; sans openai<2, pip monte
# pydantic-ai en 2.x qui exige openai 3.x et casse le provider DeepSeek.
# Version EXACTE et non une fourchette: avec >=1.30,<2.0 pip prend d'abord la
# 1.104, dont pydantic-graph exige opentelemetry>=1.38, ce que la ligne
# suivante interdit. pip redescend alors version par version (~70 aller-
# retours) et le build Railway s'eternise. Vecu le 2026-08-27.
pydantic-ai-slim[openai]==1.30.1
opentelemetry-api<1.38
huggingface-hub>=0.20,<1.0
PyMuPDF>=1.24,<2.0
Pillow>=11.0,<12.0
pdfplumber>=0.11,<1.0

# Web Push (VAPID)
pywebpush>=2.0,<3.0

# Utils
python-dotenv>=1.0,<2.0
python-dateutil>=2.8,<3.0
requests>=2.31,<3.0

# Cloud Storage
cloudinary>=1.36,<2.0
django-cloudinary-storage>=0.3,<1.0

# Optimisation planning (solveur CP-SAT de faisabilité — services/scheduling/solve_day.py)
ortools>=9.14,<10.0

# Testing
pytest>=7.4,<8.0
pytest-django>=4.5,<5.0
factory-boy>=3.3,<4.0
